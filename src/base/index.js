import httpClient, { createHttpClient } from './httpClient';
import resourceApiClient, { createResourceApiClient } from './resourceApiClient';

export {
  createHttpClient,
  createResourceApiClient,
  httpClient,
  resourceApiClient,
};

export * from './apiClient';
